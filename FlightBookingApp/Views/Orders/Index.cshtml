@model System.Data.DataTable
@using System.Data

@{
    ViewBag.Title = "Index";
}
@*@using (Html.BeginForm("OrderCancel", "Orders", FormMethod.Post))
    {*@
<h2>My orders</h2>
<table class="table table-striped table-bordered table-hover table-full-width dataTable" id="order">
    <tbody>
        <tr>
            <th> Flight Number </th>
            <th>Flight From </th>
            <th> Flight To </th>
            <th>Flight Timing from </th>
            <th> Flight Timing To </th>
            <th>Payment</th>
            <th> Status </th>

            <th></th>
        </tr>
        @foreach (DataRow row in Model.Rows)
        {
            <tr>
                @foreach (DataColumn col in Model.Columns)
                {
                    <td>@row[col.ColumnName]</td>
                }
                <td><button type="button" id="btnclick" class="btn btn-primary">Cancel</button></td>
            </tr>
        }
    </tbody>
</table>

<script>
        $(document).ready(function () {
            debugger;
            $('#order tbody').on('click', 'td', function () {
                debugger;
                var data = table.row($(this).parents('tr')).data();
                alert(data[0] + "'s salary is: " + data[5]);
            });
        });

</script>